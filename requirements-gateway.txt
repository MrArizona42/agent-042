# Minimal dependencies for the FastAPI gateway service
# This file contains only what's needed to run the gateway microservice

# Core web framework
fastapi>=0.128.0
uvicorn[standard]>=0.35.0

# HTTP client for vLLM communication
httpx>=0.28.1

# Configuration management
pydantic-settings>=2.10.1
