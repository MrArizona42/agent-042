# Python packages required by Airflow DAGs.
#
# These are installed at image build time via infra/docker/airflow/Dockerfile.
# To add or update a dependency, edit this file and rebuild the Airflow image:
#
#   cd infra/compose
#   docker compose build airflow-webserver airflow-scheduler airflow-init
#
# ── Data fetching (lightweight) ─────────────────────────────────────────
arxiv>=2.1.3
beautifulsoup4>=4.12.3
lxml>=5.3.0
requests>=2.31.0

# ── DVC versioning ──────────────────────────────────────────────────────
dvc[s3]>=3.66.1

# ── Vector-index rebuild (heavy – pulls PyTorch + transformers) ─────────
# Needed only for the build_*_index tasks.
qdrant-client>=1.12.0
sentence-transformers>=3.3.1
langchain-text-splitters>=0.4.2
