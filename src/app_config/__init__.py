"""Application-level configuration packages.

This package owns project configuration concepts such as the task/KB catalog
and runtime settings facades. Cross-cutting utilities stay under ``shared``.
"""
