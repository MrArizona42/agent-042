# MLflow tracking server dependencies.
# Compile lock file: uv --no-config pip compile --python-version 3.12 requirements-mlflow.txt -o requirements-mlflow.lock
mlflow>=3.8.1,<4
psycopg2-binary>=2.9,<3
boto3>=1.35,<2
