# Serve the Management UI under /rabbitmq/ path prefix
# so nginx can proxy it alongside other services on the same domain.
management.path_prefix = /rabbitmq

# Airflow training and evaluation tasks can legitimately run for well over
# RabbitMQ's default 30-minute consumer acknowledgement timeout.
consumer_timeout = 21600000
