# Minimal constraints for packages critical to Airflow 3.1.8 core functionality.
# These packages must match what the base apache/airflow:3.1.8 image ships
# to avoid breaking the Execution API, JWT authentication, and gRPC communication.
#
# Source: https://raw.githubusercontent.com/apache/airflow/constraints-3.1.8/constraints-3.12.txt
# Only the packages that overlap with our DAG dependencies and are critical to
# Airflow internals are listed here.  Using the FULL constraints file is not
# feasible because it pins pyarrow/dill/numpy at versions incompatible with
# the datasets library we need for evaluation.
#
# When upgrading the Airflow base image, update these pins to match the new
# constraints file for the target version.
#
# Security overrides (patched above the Airflow-shipped version):
#   cryptography: 42.0.8 → 46.0.5  (GHSA subgroup attack on SECT curves)
#   protobuf:      4.25.8 → 5.29.6  (GHSA JSON recursion depth bypass, affects <5.29.6)
#   opentelemetry: 1.27.0 → 1.28.0  (required: otel-proto 1.27 pins protobuf<5)
protobuf==5.29.6
grpcio==1.65.5
opentelemetry-api==1.28.0
opentelemetry-sdk==1.28.0
opentelemetry-proto==1.28.0
opentelemetry-semantic-conventions==0.49b0
cryptography==46.0.5
# FastAPI / Starlette must match the base image; cadwyn (Airflow's versioned-API
# layer) imports private helpers that move between FastAPI releases.
fastapi==0.117.1
starlette==0.48.0
