defaults:
  - paths: paths_config
  - experiment: train_adapter
  - _self_

hydra:
  run:
    dir: "${paths.project_root}/experiments/logs/hydra-logs/train-hydra-logs/${now:%Y-%m-%d}/${now:%H-%M-%S}"
