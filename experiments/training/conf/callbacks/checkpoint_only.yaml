defaults:
  - base_callbacks
  - _self_

# Checkpointing only: keep checkpoint callback enabled and early stopping disabled.
checkpoint:
  filename: "adapter-{epoch:02d}-{val_loss:.4f}"
  save_top_k: 3
  monitor: val_loss
  mode: min
  save_last: true
