defaults:
  - base_config
  - paths: paths_config
  - task: summarization
  - dataset: arxiv_summarization
  - model: qwen3_0_6b
  - lora: qwen_attention_mlp
  - data: sft_768_tokens
  - training: adapter_default
  - scheduler: linear_warmup
  - trainer: single_gpu
  - callbacks: checkpoint_only
  - logger: mlflow_train_adapter
  - _self_

hydra:
  run:
    dir: "${paths.project_root}/artifacts/training/hydra/${now:%Y-%m-%d}/${now:%H-%M-%S}"
